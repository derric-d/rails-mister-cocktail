<div class="home-banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('https://images.unsplash.com/photo-1547059470-3b0c7cd958a4?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=900&q=60');">
  <div class="container">
    <h1><%= @cocktail.name %></h1>
  </div>
</div>
<div class="container">
  <% @cocktail.doses.each do |dose| %>
    <%= -- dose.ingredient.name %>|
    <%= dose.description %>|
    <%= link_to "❌", dose_path(dose), method: :delete, data: { confirm: 'are you sure' } %>
  <% end %>


  <%= render 'dose_form' %>
  <%#= link_to 'Add ingredients', new_cocktail_dose_path(@cocktail) %>

<div>
  <%= link_to "back", root_path %>
</div>

</div>
